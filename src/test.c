#include "string.h"
#include <stdio.h>
#include <stdlib.h>

int main(){
    	Train_str *test = tn_str("apluh");
    	if(tn_cmp("apluh","apluh")) printf("They are equal!\n");
    	//printf("%s",test->item);
	return 0;
}
